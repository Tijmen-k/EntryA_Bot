# Full superset for local dev (one venv running the engine + dashboard +
# Discord bot). For a targeted Docker image, use requirements-engine.txt,
# requirements-dashboard.txt, or requirements-discord.txt instead.
requests==2.32.3
python-dotenv==1.0.1
loguru==0.7.2
streamlit>=1.35.0
plotly>=5.22.0
pandas>=2.0.0
psutil>=6.0.0

# Discord control-center bot
discord.py>=2.4,<3.0
reportlab>=4.0.0
matplotlib>=3.8.0
